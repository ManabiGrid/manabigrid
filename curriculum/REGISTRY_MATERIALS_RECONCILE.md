# レジストリ ⇄ materials 照合レポート

> 本ファイルは `tools/progress_index/reconcile_registry_materials.py` が生成する**照合レポート**（読み取り専用）。状態は書き換えず、レジストリの状態と materials/ の実体のズレを一覧するだけ。`docs/SPEC_progress_index.md` が将来課題とする「成果物ファイルの実在による機械判定」の第一歩。

- 実測日: 2026-07-21
- 対象: レジストリ行 478 件／教材フォルダ（.md を含む）29 件
- 照合キー: unit_id の `--` 以前（下位トピックは上位の教材フォルダを共有する）
- 判定: 本レポートは警告に留め、CI を fail させない（fail 化は将来の判断）

## 集計

| 種別 | 件数 |
|---|---|
| A. 成果物があるべきだが実体なし | 0 |
| B. 実体があるが未着手/調査済 | 0 |
| 参考. どの単元にも対応しない教材フォルダ | 0 |

## A. 成果物があるべき状態なのに materials/ に実体がない

対象状態: ドラフト／QA済／外部レビュー済／人間レビュー済／公開済。レジストリが先行し、教材フォルダ（`.md` を含む）が未作成の可能性。

- 該当なし。

## B. materials/ に実体があるのにレジストリが未着手/調査済

教材フォルダ（`.md` を含む）が実在するのに、レジストリ状態が「未着手」または「調査済」のもの。レジストリの状態更新漏れの可能性。

- 該当なし。

## 参考. どの単元・モジュールにも対応づかない教材フォルダ

`materials/*/` 直下にあり `.md` を含むが、レジストリのどの id からも（`--` 以前で）参照されないフォルダ。命名ずれ・レジストリ欠落の可能性。

- 該当なし。

