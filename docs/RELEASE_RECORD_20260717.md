---
doc_type: release_record
date: 2026-07-17
---

# RELEASE_RECORD — ベータ版 初回公開（2026-07-17）

## 凍結記録（検証可能な方式・v3）

- 対象: **gitが追跡する全ファイル**（本ファイル自身 `docs/RELEASE_RECORD_20260717.md` を除く）。git管理外のファイルは対象外
- ファイル数: 495
- 集約SHA-256: `3823ac9a8668d43e9653a3b68312e483240a99c89ce9ff85d38e0eb1a95ad108`
- 算出方法（リポジトリ直下で下のコードを実行すれば再現できます）:

```python
import subprocess, hashlib
EXCL="docs/RELEASE_RECORD_20260717.md"
out=subprocess.run(["git","ls-files","-z"],capture_output=True).stdout.decode()
files=sorted(f for f in out.split("\0") if f and f!=EXCL)
h=hashlib.sha256()
for f in files:
    h.update(f.encode()); h.update(hashlib.sha256(open(f,"rb").read()).digest())
print(len(files), h.hexdigest())
```

- 公開履歴は**クリーンルート方式**（過去の作業履歴を持ち込まない単一起点のルートコミット）。公開前の是正はこのルートの上に**通常コミット**として積みます——是正コミットの差分にも内部情報を含めないことを検疫の条件とします。凍結コミットのSHAはリリースタグ `v0.1.0-beta`（public化時に付与予定・現時点では未付与）の対象コミットとして確認できます
- 旧リポジトリの全コミットは、本リポジトリのGitHub APIで**全件取得不能**（HTTP 422 "No commit found"・2026-07-17全数照合）。取得不能の証拠値は404ではなく422

## 実施済みの検査（2026-07-17）

- 検疫対象の内部語彙（決定番号D系・HR系のID・英語の内部専用ラベル語・「内部」+「用」ラベル——定義の正本は検疫CIのコード）・秘密情報・個人情報・ローカルパスの全数検査: 0件（NFKC正規化・複合区切り込み。**区切り記号集合の正本は `.github/workflows/quarantine.yml` のコード**——文書側で「◯種」と数を要約すると数え方の版ズレが起きるため、数はコード実測を正とします）
- 制作工程のパス名（パスA〜E・B2・工程⑧ 等）は上記の検疫対象**ではなく**、`docs/METHODOLOGY.md`「内部工程名との対応」の読み替え表つきで**意図的に公開している語彙**です（件数は書きません——本記録自身の言及が件数を動かすため。残存判断の記録は BUILD_LOG 2026-07-13 の項）。実測はリポジトリ直下で次のコマンドで再現できます（対象=git追跡のMarkdown・本記録自身を除外・指標は出現数とファイル数。**このパターンが測るのは「パスA〜E」表記のみ**——「工程⑧」「B2」等の他の工程語彙は、対象語を替えて同じ形式で実測できます）:

  ```bash
  git grep -o -E 'パス[A-E]' -- '*.md' ':!docs/RELEASE_RECORD_20260717.md' | wc -l    # 出現数
  git grep -l -E 'パス[A-E]' -- '*.md' ':!docs/RELEASE_RECORD_20260717.md' | wc -l    # ファイル数
  ```
- **到達可能な全履歴の検疫（R9・R10対応）**: 全コミットのメッセージ・作者・全blob（差分の削除側を含む——各コミットのtreeを全数走査するため）に加え、**全パス名（ファイル名・フォルダ名そのもの）** も現在ツリー・全履歴の両方で、本文と同じ検出器でCIが毎回走査します。裁定済みの偽陽性は検疫CI内の **allowlist（blob SHA→裁定理由）** で管理し、内容は誰でも検証できます（現在1件: ルートコミットの図版スクリプト旧版にあった数式変数の減算式が英大文字+区切り+数字のID形に形式一致するもの。後続コミットで変数名を改名済み・内部IDでないことは外部監査R9が確認）
- 相対リンク・アンカーの全数照合: 欠落0件／レッスン→解答リンク: 126/126
- CommonMark実レンダリングでの強調崩れ（コード外の生アスタリスク）: 0件
- 図版185枚のコード来歴再現・自動検算: 全通過／ビュー生成器テスト: 全通過
- 進捗一覧はレジストリからの自動生成でバイト一致

## 既知の制限（正直な開示）

1. 一部のMarkdownレンダラ（marked等）では、数式記号に隣接する強調が生の `**` として表示される場合があります（GitHub標準のレンダリングでは0件を確認済み）。記号単独の強調の解消は改善課題です
2. 引用部分と出典台帳の機械的な1対1対応（source_id方式）は未導入です（改善課題）
3. 進捗一覧は分割済み単元の親ノードを掲載しません（親の状態は各教科レジストリ参照）
4. 検疫CIの検査パターンは透明性を優先して公開しています。実効的な防御は、ルールセット（PR必須・承認1・required checks=quarantine/quality）とメンテナによるレビューです。**ルールセットはprivateリポジトリの間はGitHubの仕様（プラン制約）で未強制です**——public化と同時に強制化される想定で、公開直後にメンテナが実測確認します。Code Ownersによるレビュー必須化は現時点では設定していません（CODEOWNERSファイルはレビュー担当の表示として機能します）。検出範囲の設計上の境界: **全検疫語彙・全IDパターンに対して、不可視文字除去とNFKC正規化を照合前に反復適用（除去→正規化→再除去）した文字列への照合を適用**し、この方法で同定できる表記ゆれまでを機械検査の対象とします（正規化で初めて不可視・結合文字に変わる文字や、正規化中に前の文字と合成されて語形を変えるかなの濁点・半濁点系修飾記号、さらに**未割当・私用領域の文字**（実行環境のUnicodeデータの版差による検出のすり抜けを防ぐため——新しい版で結合文字になった符号位置を古い版の環境は「未割当」としか知らない）も、照合前の除去対象に含めるため、2処理の適用順にも実行環境のUnicodeデータの版にも依存しません——除去対象の正本は検疫CIのコード）。**他の文字体系の同形字（ホモグリフ）や任意の符号化による偽装は機械検査の対象外**です（原理的に列挙不能のため。この層はメンテナの人間レビューで防御します）
5. ルールセットには**リポジトリ管理者（メンテナ本人）のバイパスが設定されています**——単独運営でのセルフマージ用の意図的な例外です。GitHubのIssue作成画面の「Blank issue」は**メンテナ専用**として残ります（GitHubの仕様。一般の投稿者は構造化フォームまたは外部フォームのみ）
6. CODEOWNERSの個人アカウント表記は意図的な運用です
